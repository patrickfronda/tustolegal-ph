// Torny avatar. Served as a static file from /public so it never depends on a
// large inline base64 string (a previous base64 build was corrupt and rendered
// as a broken image everywhere it was used).
export const TORNY_SRC = "/IMG_0461.jpeg";
